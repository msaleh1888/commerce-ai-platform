import { PrototypeEmptyState } from "@/components/layout";

export default function SearchPage() {
  return (
    <PrototypeEmptyState
      description="Retrieval queries, ranking evidence, and search evaluation details are intentionally deferred to their owning feature work."
      title="Search"
    />
  );
}
