import { PrototypeEmptyState } from "@/components/layout";

export default function EvaluationPage() {
  return (
    <PrototypeEmptyState
      description="Evaluation run records and metric evidence are represented as a route placeholder only; no computation is performed here."
      title="Evaluation"
    />
  );
}
