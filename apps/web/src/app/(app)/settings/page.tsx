import { PrototypeEmptyState } from "@/components/layout";

export default function SettingsPage() {
  return (
    <PrototypeEmptyState
      description="Tenant settings and member administration are placeholders until the session and settings work lands. Role context is displayed by the shell only."
      title="Settings"
    />
  );
}
