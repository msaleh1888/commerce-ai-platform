import { PrototypeEmptyState } from "@/components/layout";

export default function ProductsPage() {
  return (
    <PrototypeEmptyState
      description="Catalog browsing is a navigation destination only here. No authoritative product records are fetched or mutated by this prototype page."
      title="Products"
    />
  );
}
