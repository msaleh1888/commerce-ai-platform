import { PrototypeEmptyState } from "@/components/layout";

export default function AuditPage() {
  return (
    <PrototypeEmptyState
      description="Audit visibility is a prototype navigation destination in M2-02. Append-only audit records remain backend-owned."
      title="Audit"
    />
  );
}
