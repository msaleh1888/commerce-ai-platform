import { PrototypeEmptyState } from "@/components/layout";

export default function ReviewPage() {
  return (
    <PrototypeEmptyState
      description="Duplicate-review queue and approval prototype behavior are owned by M2-05. No catalog approval is implied on this page."
      title="Review Queue"
    />
  );
}
